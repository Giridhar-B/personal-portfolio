import AIMode from "../components/ai/AIMode";

const AIModePage = () => {
  return (
    <div
      className="min-h-screen bg-white"
    >
      <AIMode />
    </div>
  );
};

export default AIModePage;